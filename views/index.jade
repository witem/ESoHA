extends layout

block content
  h1= title
  p Welcome to #{title}
  div.widgets.container-fluid
    if (widgetArray && widgetArray.length > 0)
      each val in widgetArray
        != val
    else
      p.error Помилка отримання віджетів